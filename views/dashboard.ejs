<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Dashboard' %></title>
    <link rel="stylesheet" href="/static/css/main.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-content">
            <a href="/" class="logo">
                🚀 <span>NexusCore</span>
            </a>
            <div>
                <a href="/" class="btn btn-sm">Home</a>
                <a href="/plugins" class="btn btn-sm">Plugins</a>
                <a href="/auth/logout" class="btn btn-sm btn-danger">Logout</a>
            </div>
        </div>
    </nav>
    
    <div class="container">
        <div style="margin: 2rem 0;">
            <h1>👋 Welcome, <%= locals.user ? locals.user.name : 'User' %>!</h1>
            <p style="color: #94a3b8;">Your NexusCore Dashboard</p>
        </div>
        
        <div class="features-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem; margin: 2rem 0;">
            <div class="feature-card">
                <h3>📱 Your Profile</h3>
                <div style="margin-top: 1rem;">
                    <% if (locals.user) { %>
                        <p><strong>Phone:</strong> <%= locals.user.phone %></p>
                        <% if (locals.user.email) { %>
                            <p><strong>Email:</strong> <%= locals.user.email %></p>
                        <% } %>
                        <p><strong>Role:</strong> 
                            <span style="color: <%= locals.user.role === 'admin' ? '#10b981' : '#94a3b8' %>">
                                <%= locals.user.role %>
                            </span>
                        </p>
                    <% } else { %>
                        <p style="color: #94a3b8;">No user data</p>
                    <% } %>
                </div>
            </div>
            
            <div class="feature-card">
                <h3>🔌 Available Plugins</h3>
                <p style="margin: 1rem 0;">
                    <span style="font-size: 2rem; color: #818cf8;">
                        <%= locals.plugins ? locals.plugins.length : 0 %>
                    </span><br>
                    plugins available
                </p>
                <a href="/plugins" class="btn btn-primary">Browse Plugins</a>
            </div>
        </div>
        
        <div style="margin: 3rem 0; text-align: center;">
            <h3>🚀 Quick Actions</h3>
            <div style="display: flex; gap: 1rem; justify-content: center; margin-top: 1rem; flex-wrap: wrap;">
                <a href="/plugins" class="btn btn-secondary">🧩 Browse Plugins</a>
                <a href="/features" class="btn btn-secondary">✨ View Features</a>
                <a href="/" class="btn btn-secondary">🏠 Home</a>
            </div>
        </div>
    </div>
</body>
</html>
