<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/static/css/main.css">
</head>
<body>
    <%- include('../partials/header', { currentPage: 'login' }) %>
    
    <div class="container">
        <div class="login-container">
            <h2 class="login-title">🔑 Login to NexusCore</h2>
            
            <% if (error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
            <% } %>
            
            <form method="POST" action="/auth/login">
                <div class="form-group">
                    <label class="form-label">Phone Number</label>
                    <input 
                        type="tel" 
                        name="phone" 
                        class="form-control" 
                        placeholder="+923288055104" 
                        required
                        value="<%= phone || '' %>"
                    >
                    <small style="color: #94a3b8; display: block; margin-top: 0.25rem;">
                        Enter your registered phone number
                    </small>
                </div>
                
                <button type="submit" class="btn btn-primary" style="width: 100%;">
                    Send OTP
                </button>
            </form>
            
            <div style="text-align: center; margin-top: 1.5rem;">
                <p style="color: #94a3b8;">
                    New user? <a href="/auth/signup" style="color: #818cf8;">Sign up here</a>
                </p>
                <p style="color: #94a3b8; font-size: 0.9rem;">
                    Or <a href="/auth/login/admin" style="color: #818cf8;">login as admin</a>
                </p>
            </div>
        </div>
    </div>
</body>
</html>
