<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/static/css/main.css">
</head>
<body>
    <%- include('../partials/header', { currentPage: 'admin' }) %>
    
    <div class="container">
        <div style="margin: 2rem 0;">
            <h1>⚡ Admin Dashboard</h1>
            <p style="color: #94a3b8;">Welcome, <%= user.name %></p>
        </div>
        
        <!-- Stats Cards -->
        <div class="features-grid">
            <div class="feature-card">
                <h3>👥 Total Users</h3>
                <p style="font-size: 2.5rem; color: #818cf8; margin: 1rem 0;">
                    <%= stats.totalUsers || 0 %>
                </p>
            </div>
            
            <div class="feature-card">
                <h3>🧩 Active Plugins</h3>
                <p style="font-size: 2.5rem; color: #10b981; margin: 1rem 0;">
                    <%= stats.totalPlugins || 0 %>
                </p>
            </div>
            
            <div class="feature-card">
                <h3>📊 Daily Logs</h3>
                <p style="font-size: 2.5rem; color: #f59e0b; margin: 1rem 0;">
                    <%= stats.dailyLogs || 0 %>
                </p>
            </div>
            
            <div class="feature-card">
                <h3>📱 SMS Sent</h3>
                <p style="font-size: 2.5rem; color: #ec4899; margin: 1rem 0;">
                    <%= stats.dailySMS || 0 %>
                </p>
            </div>
        </div>
        
        <!-- Admin Actions -->
        <h2 style="margin: 3rem 0 1rem;">⚡ Admin Actions</h2>
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1rem;">
            <a href="/admin/plugins" class="btn btn-primary" style="padding: 1.5rem; text-align: center;">
                <div style="font-size: 2rem;">🧩</div>
                <div>Plugin Manager</div>
            </a>
            
            <a href="/admin/plugins/upload" class="btn btn-secondary" style="padding: 1.5rem; text-align: center;">
                <div style="font-size: 2rem;">📤</div>
                <div>Upload Plugin</div>
            </a>
            
            <a href="/admin/users" class="btn btn-secondary" style="padding: 1.5rem; text-align: center;">
                <div style="font-size: 2rem;">👥</div>
                <div>User Management</div>
            </a>
            
            <a href="/admin/settings" class="btn btn-secondary" style="padding: 1.5rem; text-align: center;">
                <div style="font-size: 2rem;">⚙️</div>
                <div>Settings</div>
            </a>
        </div>
        
        <!-- Recent Plugins -->
        <% if (plugins && plugins.length > 0) { %>
            <h2 style="margin: 3rem 0 1rem;">📦 Recent Plugins</h2>
            <div class="features-grid">
                <% plugins.slice(0, 4).forEach(plugin => { %>
                    <div class="feature-card">
                        <h3><%= plugin.name %></h3>
                        <p style="color: #94a3b8; margin: 0.5rem 0;">
                            <%= plugin.description || 'No description' %>
                        </p>
                        <div style="margin-top: 1rem;">
                            <span style="color: <%= plugin.enabled ? '#10b981' : '#94a3b8' %>">
                                <%= plugin.enabled ? '✅ Enabled' : '❌ Disabled' %>
                            </span>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>
</body>
</html>
